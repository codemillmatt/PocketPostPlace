Create a simple navigation structure on the post detail page. It should accomplish the following:

- Above the post's title there should be a link that lets the user go back to the site's main screen.
- On the bottom left of the post - after all the text - there should be a "previous" that lets the user navigate to a post that was created earlier (by date) than the one they are viewing.
- On the bottom right of the post - after all the text - there should be a "next" that lets the user navigate to a post that was created later (by date) than the one they are viewing.
- If there is not a "previous" post - then the previous link does not appear.
- If there is not a "next" post - then the next link does not appear.
- Treate posts with the newest or latest pub date as the "next" or newer posts.